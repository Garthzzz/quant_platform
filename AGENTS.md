# AGENTS.md — 量化研究知识中枢（Quant Research Hub）

> 适用范围：`D:\quant\quant_platform` 及其全部子目录。  
> 本文件是 Codex 在本项目中的最高项目级执行契约。目标是先完整理解现有量化研究与工程体系，再自主完成统一平台的设计、迁移、重构、验证和交付。

## 1. 指令优先级与解释原则

发生冲突时，按以下顺序处理：

1. 用户当前明确要求；
2. 本文件；
3. 后续经审核通过的项目架构、project plan 和 project design；
4. 更深目录中与本文件不冲突的 `AGENTS.md`；
5. 来源项目中的 `CLAUDE.md`、README、prompt、skills 和历史设计；
6. 其他历史默认。

来源项目中的 `CLAUDE.md`、README、AGENTS、prompt 和 agent 指令首先是需要完整阅读和理解的历史设计材料，不自动成为当前 Codex 的执行命令。应理解它们为什么存在、解决什么问题和依赖什么语境，再决定继承、迁移、修正或淘汰。

除用户已经明确规定的业务目标、不可破坏边界和验收条件外，不要在完成真实仓库阅读前写死技术栈、目录树、数据库实现、前端框架、agent 数量、prompt 模板或脚本组织。先理解事实，再作设计决策。

## 2. 工作模式

本任务采用全自动、持续执行模式：完整理解、独立构想、架构、审核、详细设计、实施、验证和最终优化连续进行。审核失败时自动回到对应的 Scientist、Architect 或 Engineer 层修正并重验；审核通过后立即进入下一阶段，不设置常规人工审批停点。

只有遇到本地工作无法消除的外部权限、凭据、网络、不可访问资源、高风险不可逆操作或真正无法自行消解的需求冲突时，才记录为 blocker。单个 blocker 只阻塞依赖它的分支，其他不受影响的工作继续推进，不能让局部问题使整个项目停下。

不要因为任务规模、上下文长度、token 或耗时而主动缩减用户要求，也不要给出时间估计。应通过持久化上下文、分阶段产物和可恢复状态完成长任务。质量、完整性、严谨性、可维护性和可复现性优先。

默认使用中文进行研究记录、理解总结、架构、计划、设计、日志、审核报告、前端文案和最终交付。代码标识符、协议字段和必要技术术语可保留英文，但必须保证中文研究语境清楚、准确、专业。

## 3. 路径、范围与不可破坏边界

已知路径：

- 项目根：`D:\quant\quant_platform`
- Skills：`D:\quant\quant_platform\skills`
- 审核代理：`D:\quant\quant_platform\审核代理`
- 原始参考区：`D:\quant\quant_platform\reference`
- 原论文研究系统：`D:\quant\quant_platform\reference\proj2`
- 原量化研究成果：`D:\quant\quant_platform\reference\archive`
- 行研参考系统：`D:\quant\industry_demo`

这些路径和内部结构可能已经变化。启动后先发现真实文件系统，建立实际路径映射；路径不一致时应研究并修正认知，不能静默跳过内容。

以下边界是硬约束：

1. `D:\quant\quant_platform\reference` 是只读原始参考区，不得直接修改、重命名、移动、格式化、覆盖或写入任何产物。
2. `D:\quant\industry_demo` 默认只用于阅读、比较和借鉴，不在其目录内实施本项目改动。
3. 新系统必须建设在 `quant_platform` 的可写区域，不能覆盖原始 `proj2` 或 `archive`。
4. 在新实现完成等价性和回归验证前，不得删除或替换旧实现。
5. 所有迁移和重构应可追溯、可回滚，不得使用不可恢复的破坏性操作换取便利。
6. `archive` 研究 Markdown 的原始正文必须保持逐字节不变。任何元数据、摘要、索引、引用、评论和展示优化必须与原文分离；前端不得借解析或格式化之名改写正文。
7. **VM 写入根边界：**生产 VM 上本项目的任何新增或修改内容，包括代码、Git checkout、candidate、release、`incoming`、外置 state、checkpoint、backup、audit、lock、manifest、部署工具、日志和临时文件，都必须位于唯一根目录 `D:\quant\quant_platform` 内。不得在 VM 的 `D:\`、`D:\quant` 或其他同级/上级目录落任何项目文件或临时产物；更不得向 VM C 盘新增、覆盖或修改内容。现有 `C:\quant_platform`、`C:\quant_platform_data` 及其服务仅允许只读核验，直到既定 writer handoff 门禁通过；任何工具在执行 VM 写操作前必须机械验证目标解析后仍位于该 D 根内，并在执行后审计实际写入路径。独立故障域 `RECOVERY_ROOT` 位于另一主机，不受“放到生产 VM D 根”误解为同机其他目录。

## 4. 第一阶段：完整理解三个来源体系

在形成实质性架构或开始正式重构前，必须先盘点整个 `quant_platform`，然后依次完整阅读并理解以下三个体系。

### 4.1 `reference\proj2`

首先阅读 `D:\quant\quant_platform\reference\proj2`。沿其 README、`CLAUDE.md`、入口脚本、配置、schema、数据库、前端、prompt、skills、agent、测试和内部引用逐层追踪，理解全部项目特有内容及相互关系，尤其包括：

- `/papers` 的论文投递、发现、读取、分析、总结、入库和前端映射；
- Claude/`CLAUDE.md` 驱动方式、agent loop、prompt 组织、验证和失败处理；
- 数据库、来源、引用、任务状态和运行方式；
- 量化架构设计系统及其工作流；
- 现有功能、隐含假设、有效设计、技术债、迁移风险和实际可运行行为。

不得只读 README 或少数入口，也不得把其他模型生成的总结当作对源码和配置的替代。

### 4.2 `reference\archive`

完整阅读 `D:\quant\quant_platform\reference\archive` 中全部量化研究成果。理解每篇研究的主题、问题定义、论证结构、相互关系、研究进度、未决问题、提到或依赖的论文，以及这些成果共同构成的量化研究脉络。

`archive` 是新系统的首页、主体和核心知识资产，不是附属资料。

### 4.3 `industry_demo`

完整阅读 `D:\quant\industry_demo`，将其视为目前最新、最成熟的设计参考。必须理解其从资料和 papers 引入，到读取理解、数据库、分析、写作、`^src` 引用、前端映射、检查和循环修正的完整链路，尤其关注：

- papers 的发现、读取、理解、入库和状态管理；
- agent、prompt、审核代理和验证 loop 的组织方式；
- 来源证据与生成内容的边界；
- 引用协议及前端交互行为；
- 长篇内容的前端组织和阅读体验；
- 失败恢复、增量处理和最终放行机制。

借鉴其成熟原则和经过验证的机制，但不得机械复制行业研究的业务假设、数据模型或目录结构到量化研究系统。

### 4.4 阅读覆盖和辅助记忆

用户要求的“完整阅读”是实质性要求。所有项目特有的源代码、配置、文档、prompt、schema、迁移、测试和重要数据样例都应被阅读或检查。标准第三方依赖、构建产物、缓存、重复生成物和无法文本阅读的二进制可以分类排除，但必须记录排除规则和覆盖状态，不能静默忽略。

在参考区之外建立可恢复的 context cache、阅读日志、文件索引、系统结构图、关键发现、风险、决策和进度记录。大文件可以分块阅读并做摘要，但关键设计决策和验证结论必须回到原文件复核，缓存不能取代来源。

完成来源理解时，应有可审计证据说明：读了什么、为何排除什么、各系统实际怎样运行、哪些结论来自源码或文档、哪些仍是推断或待验证。

## 5. 目标系统的总体关系

目标是建设一套正式、统一、Codex 驱动的量化研究知识与工程平台：

- `archive` 是首页、研究主体和用户最主要的阅读入口；
- `archive` 相关论文及其证据体系构成独立的研究引用与论文库；
- 迁移后的 `proj2` 是外部量化论文研究和量化架构研究的支持系统；
- `industry_demo` 提供成熟设计参考，但不是被直接复制的目标系统；
- `reference` 只保留原始资料和旧系统，始终只读。

整体架构应让研究正文、结构化元数据、论文证据、生成分析、评论、进度和前端视图边界清晰，同时能够自然联动。

## 6. 必须完成的业务能力

### 6.1 迁移并优化 `proj2` 论文研究系统

在完整理解 `proj2` 后，在可写区复制、修正并正式命名一套新的论文研究子系统。正式名称、目录和实现由 Architect 根据真实项目确定并记录映射。

必须满足：

- 保留原有完整有效功能和原有使用方式；
- 论文仍可通过放入 papers 库自动进入读取、分析、总结、入库和前端流程；
- 保留原有量化架构设计能力；
- 将主要依赖 Claude/`CLAUDE.md` 的驱动机制迁移为 Codex 可稳定执行的机制；
- 不做简单文件名替换、prompt 直译或表面兼容；
- 基于真实问题优化论文阅读深度、证据提取、分析理解、验证 prompt、失败回路、结果一致性和自动放行；
- 使用行为、数据和回归测试证明迁移前后的功能等价，并记录有意改进的差异。

### 6.2 建设 `archive` 研究主体和前端

为 `archive` 建设结构化、交互式、长期可扩展的研究主体系统。前端应科学、克制、清晰、具有高信息密度，适合量化研究员阅读，不追求装饰性效果。

可以通过展示层改善超长文档的导航、章节层级、折叠展开、引用查阅、搜索和跨研究跳转，但源 Markdown 和研究核心正文一个字都不能改。任何摘要、标签、目录、阅读辅助和结构化结果均应作为独立数据层或展示层存在。

未来新增的研究 Markdown 应自动进入发现、解析、元数据整理、论文线索提取、论文处理、数据库更新、前端映射和验证流程，不得依赖手工重建整个系统，也不得在自动处理时改写正文。

### 6.3 建设 `archive` 独立论文与引用系统

完整阅读 archive 时，必须识别研究中正式引用、文字提及、作为依据、可合理定位或明确指向的 papers，并完成以下工作：

1. 生成一份可审计的 TXT 论文清单，保留论文线索、所在研究、识别状态和处理状态；
2. 对可确认且可合法获取的论文进行抓取，存入独立的 `research_papers` 资源区；
3. 对无法确认、无法获取、链接失效或存在歧义的论文保留明确状态和原因，不得编造或静默丢弃；
4. 为这批论文建立区别于 `proj2` 数据库的独立数据库；
5. 建立论文发现、读取理解、元数据核验、入库、引用绑定、前端展示和验证机制；
6. 使用已抓取的 `research_papers` 完成一次完整回放和检测，证明整个链路可用。

数据库词条至少应表达：论文类别（量化、数学、机器学习或其他合理类别）、核心结论、发表时间、作者、机构、外部链接、本地文件直达链接，以及它与 archive 研究的引用或关联关系。可以根据真实需求扩展，但不得删减用户明确要求的内容。

`^src` 引用的显示、跳转和交互体验应以 `industry_demo` 当前实际行为为验收基准，做到一致且可追溯。若真实技术条件导致无法直接复用实现，应保持等价的用户体验和引用语义，并记录原因，不能自行弱化要求。

论文、作者、机构、时间、结论、链接和引用关系不得臆造。事实、来源原文、模型推断和待核验信息必须明确区分。

### 6.4 评论系统

每项研究都应支持持久化评论，至少保存并显示评论者、内容和时间。评论者选项固定包括：

- 张正泽；
- 宋定坤；
- 其他，选择后允许手动输入姓名。

评论数据必须与研究正文和论文证据分离，不得污染或改写原文。

### 6.5 研究进度 Dashboard

首页增加研究进度 Dashboard，并与真实研究内容保持一致：

- 已完成 topic：自动发现或更新，展示简短总结、对应页面和快速链接；
- 尚未完成 topic：由研究员维护，不要求总结或链接；
- 暂时搁置 topic：由研究员维护，可记录状态说明，不要求页面链接。

Dashboard 应随新增研究和状态变化增量更新，不能成为需要单独重复维护、容易失真的静态清单。

### 6.6 全局集成和最终体验

完成各子系统后，必须进行全局集成、数据一致性检查、功能回归、自动化链路验证和前端最终优化。特别关注超长研究内容的阅读负担，允许在展示层进行必要的结构重构，但不能改变研究正文和证据含义。

最终系统必须证明旧功能得到保留、新功能真实可用、新研究和新论文可以增量进入系统、引用可追踪、评论和 Dashboard 可持续使用、失败不会被静默掩盖。

## 7. 角色、设计产物与连续执行链

角色是不同工作视角，不是互相隔离的形式标签。实际执行可以合理并行，但不能跳过其核心职责。

### `[SCIENTIST]`

负责全量阅读、系统还原、研究语境理解、跨项目比较、需求建模和独立构想。必须解释原有设计为什么存在、哪些值得继承、哪些已经过时、哪些只适用于原场景，以及目标系统真正需要解决的问题。

Scientist 未完成可审计的来源理解前，不得凭想象进入正式架构和大规模编码。

### `[ARCHITECT]`

基于 Scientist 的真实发现，形成总体架构和 project plan。应确保模块边界、数据流、接口、命名、schema、状态、上下文、引用协议、迁移关系、前后端契约、失败路径和验证策略一致，并主动寻找更简单、更稳定的架构方案。

project plan 负责说明实施阶段、依赖、优先级、风险、迁移顺序和质量门禁；不能用计划代替架构推理。

### `[SCIENTIST + ARCHITECT + ENGINEER]`

在总体架构和 plan 通过审核后，联合形成更接近代码层的 project design。该设计应把关键模块、接口、变量和 schema 命名、数据生命周期、代码边界、兼容策略、增量机制、测试和回退方式说明清楚，并保证跨模块上下文一致。

不要在来源尚未验证时提前写死细节，但进入实施前，关键契约不能继续含糊。

### `[ENGINEER]`

按已审核的设计持续实施，并在真实代码与设计不一致时记录证据、更新设计和重新审核。重要模块完成后及时补充测试和验证，保持系统可运行、可回退，不把全部问题推迟到最终集成。

每项重要修改都必须记录：做了什么、为什么这样做、影响哪些模块、如何验证。

### `[INDEPENDENT VERIFIER]`

关键放行使用新的、相对独立的审核代理。审核代理应基于用户要求、来源证据、架构、代码差异、数据产物和实际测试独立判断，不直接继承执行代理的自我结论，也不能仅审阅总结而不检查证据。

审核失败时，应给出最少但充分、可操作且带证据的缺陷清单，明确返回 Scientist、Architect 或 Engineer 的哪一层；修正后重新运行审核。新的审核必须基于新的改动或证据，禁止无变化地反复执行相同 review。

## 8. Agent loop 与关键放行

agent loop、审核代理和验证脚本是自动化生产的重中之重，但只放在真正决定质量的边界，避免为了形式制造重复代理、重复 prompt 和无价值循环。

至少应覆盖以下关键风险门禁，具体合并和实现方式由完成来源研究后的 Architect 决定：

1. 三个来源体系的阅读覆盖、系统理解和证据是否足够；
2. 总体构想、架构、project plan 和 project design 是否完整一致；
3. `proj2` 迁移是否保留原使用方式和完整功能；
4. archive 原文是否零改动，论文发现、抓取、理解、数据库和 `^src` 是否可追溯；
5. 新研究导入、评论、Dashboard、前端和所有链接是否端到端可用；
6. 全局回归、异常路径、数据完整性、增量运行、失败恢复、维护性和最终体验是否通过。

只有通过相应 gate 的产物才能进入下一层或正式生产路径。失败必须修正并重跑，不能以解释、承诺或“基本通过”代替验证。

自动化链路应支持增量运行、合理的幂等性、重复执行、来源追踪、失败恢复和明确状态。不得出现静默失败、未经说明的占位实现、只在演示数据上成立的伪完成，或无客观证据的完成声明。

## 9. Skills、审核代理和 OpenSpec

启动时完整检查 `D:\quant\quant_platform\skills`，理解相关 skill 的触发条件、输入输出和适用边界，不凭名称猜测，也不为形式调用全部 skill。

必须读取并实际启用：

`D:\quant\quant_platform\skills\输出层\latex-chinese`

虽然该 skill 原本主要面向 LaTeX，但本项目的 Markdown、HTML、前端文案、研究摘要、表格、数学公式和中文排版应继承其有效原则。应在保留原 skill 的前提下进行项目级适配，不能机械照搬不适用于前端的部分，也不能破坏旧版本。

必须验证当前 Codex 是否能够实际发现、加载和调用相关 skills。路径存在不等于已经激活；若现有结构与 Codex 的发现机制不兼容，应建立最小、可追踪的兼容层或映射，并用实际调用证明有效。

完整检查 `D:\quant\quant_platform\审核代理` 的现状。可以根据本任务复用、修改、补充或淘汰旧审核设计，但必须记录原因、影响和兼容性，审核代理和验证脚本自身也要接受测试。

可以使用 OpenSpec 协助复杂变更、计划和追踪，但不得过度依赖。OpenSpec 不能替代源项目理解、架构推理、决策记录、进度状态和实际验证，也不能成为唯一事实来源。

## 10. 持久化上下文、日志和跨 session 恢复

本任务内容极多，必须在参考区之外维护简洁但足够恢复状态的持久化上下文。具体文件和目录由 Architect 结合现有项目决定，但至少要保留以下信息：

- 文件和阅读覆盖索引；
- 三个来源系统的结构与行为模型；
- 用户需求和完成状态；
- 当前构想、架构、project plan 和 project design；
- 关键决策、被否决方案和原因；
- 迁移映射、当前进度和下一步；
- 重要修改及验证结果；
- 各 gate 的审核证据、失败项和修正状态；
- 外部 blocker、遗留问题和获取失败项。

日志用于记忆、审计和恢复，不应写成低价值的逐步流水账。记录重点是“为何这样理解、为何这样修改、影响什么、证据是什么、下一步是什么”。

新 session 启动时，先读取根 `AGENTS.md`、当前进度、需求状态、阅读覆盖、关键决策、最新架构和活动阶段文档，再继续工作。不得只依赖聊天上下文，也不得在已有可靠缓存时无意义地从头重复全部阅读。

## 11. 每轮自我审查

完成每个重要的理解、构想、设计、实现或验证循环前，应真正检查：

- 是否完整理解了相关源文件、实际行为和原设计目的，而不是只理解表面流程；
- 是否遗漏了文件、边界、数据、引用、失败路径、增量更新、前端或长期维护关系；
- 当前判断是否被未经验证的假设、参考系统的场景偏见或过早技术选择限制；
- 是否存在更简单、更一致、更稳定、更容易审计的方案；
- 事实、原文、推断和生成内容是否被严格区分；
- 是否有客观证据证明当前阶段达到放行标准，而不是只满足了自己写出的计划；
- 是否还有价值显著高于新增复杂度的改进没有覆盖。

保持质疑，发现问题就修正；确认通过后自动继续下一阶段。不要机械复述这些问题，应在设计、代码、日志和验证证据中体现实际审查结果。

## 12. 完成定义

只有以下事项全部有客观证据时，才能标记整个项目完成：

1. `quant_platform` 和三个核心来源体系已完成可审计的全面阅读与理解；
2. `reference` 和 `industry_demo` 未被修改；
3. `proj2` 已在可写区迁移为正式命名、Codex 驱动的系统，原使用方式、完整功能和量化架构设计能力经验证保留；
4. archive 已成为首页和研究主体，源 Markdown 逐字节未改，前端展示忠实且适合长篇量化研究阅读；
5. archive 中论文线索已形成 TXT 清单，获取状态可审计，可获取论文已进入 `research_papers`；
6. archive 独立论文数据库、论文读取与验证机制、`^src`、外部链接、本地直达链接及研究关联完整可追溯，并与 `proj2` 数据库正确隔离；
7. 新研究 Markdown 能自动进入发现、论文提取、入库、前端映射和验证链路；
8. 评论系统和研究进度 Dashboard 真实可用并可持续更新；
9. 自动化流程通过增量、重复执行、失败恢复、异常路径、数据一致性、回归和端到端验证；
10. 关键 agent loop、审核代理和验证脚本工作有效，所有失败项均已关闭或明确记录为不可消除的外部 blocker；
11. 没有静默失败、未经说明的占位实现、内容破坏、编造引用或无证据的完成声明；
12. 架构、project plan、project design、运行方法、关键决策、修改原因、验证证据、遗留项和最终报告均以中文完整留档。

在完成定义满足之前，持续自主研究、实施、审核、修正和优化，不因任务规模而自行提前结束。
