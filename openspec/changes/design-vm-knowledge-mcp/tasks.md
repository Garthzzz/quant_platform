## 1. Stage 0：权威基线与发布核心

- [x] 1.1 冻结现网 V39 的代码、模板、CSS/JS、四个只读库、PDF、图片、对象、Paper Lab、研究工作区种子和代表性页面/API/浏览器基线。
- [x] 1.2 GitHub 在 Stage 0–5 保持 Public；建立根 `.gitignore`、`.gitattributes`、allowlist 与 reference/internal-research/secret/SQLite/PDF/object/generated-state/release 禁入门禁。
- [ ] 1.3 重新定义并实现本地无环 identity：immutable `R=release_manifest` 只含代码/内容/资源/索引/知识和 state compatibility，`active_release→R_active`；`local_prior_binding→R_active/R_prior`；activation/rollback receipt 只指向结果 pair/result，failure receipt 显式绑定 operation、原 pair/target candidate/result，并严格区分 activation 新 candidate、exact prior rollback target 与空 D pair bootstrap，cleanup receipt 只指向保留 pair/精确移除目标/result，且均不作 pointer。加入 schema/graph linter，拒绝 R 反向引用 active、binding、receipt 或动态切换信息。
- [ ] 1.4 按新合同重构 VM deploy CLI 核心：精确 release、`.partial`、hash、candidate 启动、active/prior pair 切换、同一 D state 回退、崩溃重放和终态清理；移除对已取消发布保护权威的依赖。
- [ ] 1.5 实测本地测试入口、生产 VM 网络/权限/端口、SQLite candidate 隔离副本协议、`cutover_budget_ms` 以及 exact D 根内 active/prior/control/state 真实路径。
- [ ] 1.6 按新范围重新验证生产 VM 闭合 write-set：唯一根 `D:\quant\quant_platform`，覆盖 checkout/incoming/releases/control/state/audit/locks/logs/tmp/tooling/bytecode 与隔离副本；拒绝 D 上级/同级、C 与 reparse/junction 逃逸，并以执行前 canonical preflight、执行后 write-set audit 和正反 fixture 证明不存在遗留外部写路径。

## 2. Stage 1：V39 D 盘兼容 bootstrap

- [ ] 2.1 按 V39 inventory 全量同步 Git 外 PDF/图片/对象/数据库/内容快照到 D `incoming`，验证 path/size/SHA 和链接可达。
- [ ] 2.2 用 SQLite online backup 生成隔离 candidate state，完成 integrity/foreign-key/schema/count/hash 与空/非空 comment fixture。
- [ ] 2.3 完成 legacy renderer 的桌面/窄屏截图、关键 DOM、公式、表格、引用、搜索、评论、Dashboard、Paper Lab 和门禁回归。
- [ ] 2.3a 在旧 C V39 继续占用 8765 时，用 `.240` 的 D-root tooling、loopback 隔离端口和 tmp 状态副本真实启动 exact D candidate；验证 release/manifest/snapshot 与关键浏览器/API，证明不改 active pointer、生产 state、C 盘或 writer authority，并生成清理审计。
- [ ] 2.4 建立生产 VM exact D 的 release 保留布局与机械 inventory：终态只允许 `active_release.json` 指向的 active、`local_prior_binding.json` 指向的恰一 prior，以及当前 D state；活动 attempt 期间允许一个有 journal 的瞬态 candidate。验证 canonical path、regular file、无 reparse、manifest closure 和第三 release 拒绝语义。
- [ ] 2.4a 收缩 installed wheel、CLI、config、schema、定时任务与正式导出面到 active/prior 本地版本合同：删除不再属于产品范围的跨主机保护 producer/consumer、状态周期复制、旧 public symbols/entry points 和相关权威判断；以 source/fresh-wheel exact inventory、AST/call-graph、零外部写路径 spies 证明这些能力不可发现、不可导入、不可调用。此前围绕旧权威取得的 PASS 不再作为 2.4、发布或 handoff 的完成证据。
- [ ] 2.5 在生产 VM 精确 D 根内建立一棵冻结 V39 immutable baseline `R0` 和一棵具有真实不同 manifest/release identity 的 successor candidate `R1`；不得复制 V39 内容或伪造身份填充 prior。在 writer fence 内让二者对最终 D state 分别通过浏览器/API/hash/schema验收，以 `bootstrap_first_pair` durable journal 和 activation-family terminal receipt 完成尚未对外的 `active: absent→R0`（唯一允许 `prior=null` 的成功形状且不授权 ingress），再以正常激活协议形成 `R1` active + `R0` prior 和 `local_prior_binding`。D 尚未接收外部写入时失败可安全退回未变化的 C；只有真实 pair 通过后才开放 D 流量并使 C 永久退出 writer authority。
  - 2026-08-29 实现进度：本地已加入 v2 V39 freezer/prepare、v4 `bootstrap_first_pair` 生产控制器、空指针 CAS、baseline transient qualification、8765/C-writer 现场 fence、bootstrap success/failure terminal、首次 R1 失败后不开放 R0 的 pre-ingress failure terminal，以及 `qrh-writer-handoff` 内嵌的 R0→R1 固定 bridge。生产 handoff 不再接受外部 runtime/clock/verifier/checkpoint/state-source 注入；test-only persistence/checkpoint/service/bootstrap/controller 均在触碰文件或服务前拒绝 production D 及别名。失败重试会验证并复用唯一 sealed non-ingress R0，而不把 `R0/null` 当稳态启动。writer/bootstrap/controller 专项 32 项、Windows service 28 项、产品真实调用图 210 项均通过；任务仍保持未完成，等待真实 R1 产物、生产 VM exact-D 执行、浏览器/API/write-set 现场证据与独立 verifier 最终 PASS。
  - 2026-08-29 增量封口：handoff journal 升级为 closed v4 crash-state machine；停止 C、live-authorized checkpoint、D state replace 和 `d_bridge_pending` 均有相邻 durable phase，且从 `final_checkpoint_created` 起持久绑定 final-C/pre-D checkpoint ID 与 manifest hash，拒绝同 ID 完整重签 ABA。checkpoint 到 D state 的产品路径只传递固定句柄读取并完成内存校验的 SQLite bytes，目标 state、候选 probe 与 transient cleanup 均固定父目录并使用相对写入/替换/删除。失败路径按“不可变 failure receipt→hash-bound terminal journal→cleanup”提交，cleanup 后进程退出仍可由 fresh finalize 唯一收敛。所有 pre-ingress crash cut 恢复 pre-D state 与 exact C，已开放 D 只能用 exact committed `R1 active + R0 prior + steady_current` 证明向前完成。相关 checkpoint/writer/service/deploy 定向 92 项通过；任务仍等待完整回归与生产 VM 现场执行，不提前勾选。
  - 2026-08-29 failure terminal 增量封口：failure receipt 升级为 v2 canonical self-hash，失败 terminal journal 精确绑定 receipt hash；receipt-pending crash cut 的 fresh finalize 必须重新现场证明 D closed 与 exact C restored。整体重签篡改 rollback/D-open 字段、terminal binding 漂移或 D 仍开放均保留 fenced journal，不得从 receipt 布尔值推导安全。writer/checkpoint/service 整组 68 项通过，独立 verifier 对两类 tamper 反例复验通过；任务状态不变。
- [ ] 2.6 将 2.1–2.5、writer fence、candidate/prior 对当前 D state 的兼容性和 D-root write-set audit 设为首次生产 handoff 门禁；未通过时只允许隔离候选验证，不得开放 D 生产流量、转移 writer authority或清理仍用于 C→D 过渡的 V39 材料。
- [ ] 2.7 获批 apply 后才演练外部流量/写入 fence、最终复制，以及 `R0` baseline→`R1` successor 的首次 D handoff；证明 D 未接收外部写入时失败可安全退 C，真实 active/prior pair 通过并开放流量后 C 永久退役。
- [ ] 2.8 对 2.5 建立的 `R1` active + `R0` prior 演练只使用同一 D state 的 local prior rollback；验证回退后 active/prior 角色交换、current/event/revision/actor/time 不倒退、终态仅保留两棵生产 release，随后才按过渡清理合同处置旧 V39/C 材料。

## 3. Stage 2：一次 publish 自动发布

- [x] 3.1 实现受控本地 publish CLI：冻结 Git 外来源、运行本地 gate、执行一次 push、等待 exact-SHA GitHub-hosted CI。
- [x] 3.2 实现 candidate 的增量 hash 传输与 VM deploy CLI 调用，取消人工制作/传输广播包。
- [x] 3.3 实现 latest-only coalescing、VM 串行 lock、运行中部署不取消、旧 pending 被最新 main 替换的明确状态与审计。
- [x] 3.4 将 `publish` 固定为本版唯一生产入口，删除 watcher/pre-push deployment hook/self-hosted runner/bare receive 的实现任务和开放选择。
- [ ] 3.5 在 candidate 激活前验证当前 active 可成为唯一 prior、candidate 与当前 active 均兼容同一 D state，并预构造只引用 candidate/current-active 的 pair intent；release manifest 与 active pointer 不得含 binding/receipt。任一 hash、路径、state compatibility 或两版本保留预算不满足时 fail closed。
- [ ] 3.6 只有 active pointer 切换、candidate 启动、post-activation health/关键功能/writer fence 与新 `local_prior_binding` 全部成功后才生成普通 `activation_receipt`；失败只有在原 pointer、binding、active service、writer fence 与当前 D state identity 分别以 sealed observation 证明已恢复或未变化后，才生成绑定 exact journal operation、terminal 前最后一个合法 non-terminal 失败阶段、原 pair/target candidate、五项 observation hash 与聚合 hash 的 `failure_receipt`。`activate_successor` target 必须与原 pair 不同，`rollback_to_prior` target 必须恰为原 prior，`bootstrap_first_pair` 必须从空 D pair 开始。首次 `absent→R0` 使用 activation receipt 家族的 `bootstrap_first_pair` 特例，必须证明原 pointer/binding absent、ingress closed、旧 C writer fenced、最终 D state 与 R0 live/writer identity，且不得授权 ingress 或 cleanup。pending journal 仅作 crash coordination，以 SCM transient role/attempt/phase/nonce 精确授权启动；qualification 只由 B2 one-shot seam 在最后 live replay 后消费并推进相邻 verified revision，result/aggregate 已存在但 revision 未推进时必须失败而不得恢复资格。普通 reboot 只从无 pending 的 distinct steady workspace/authorization 启动；transient 与 steady child 均须在执行首条 Python 前通过 creation-time job list 原子加入 non-inheritable `KILL_ON_JOB_CLOSE` Job Object，覆盖 host crash 后的 orphan/writer/listener 回收。覆盖 journal/pointer/start/probe/binding/receipt/cleanup 每个 crash cut，revision 0 必须是 durable intent，普通 reboot不得启动 pending candidate，重放不得产生两个终态 receipt，成功 terminal 前不得授权 cleanup；cleanup 必须跟随同 attempt 的非 bootstrap 成功 terminal 且保留 pair 完全一致。
  - 2026-08-29 实现进度：ordinary failure terminal 已取消伪 observation fallback，改为真实 steady SCM/endpoint/writer observation、持久 recovery authorization 与失败 candidate 精确清理；bootstrap 具有独立 absent-control failure closure；v2 `candidate_only` 只探测 `incoming/*.partial` 且不 finalize、不写 pointer/binding/journal/receipt。完整 crash-cut、真实 Windows owner-crash 与现场 VM 资格尚待后续门禁，故本任务不提前勾选。
  - e.4 Round8 子门禁须分别证明：controller B2 observation owner 与 SCM-host-local launch/lifetime owner不跨进程混用；SQLite CREATE_NEW creator handle直接成为mutable guard且无reopen空窗；JOB_LIST与仅含admission-pipe-read/log的HANDLE_LIST同属一个`STARTUPINFOEXW`，job/admission-write handle不继承；Win10/Server2016+与compatible outer-job preflight；新host以全机双snapshot排除旧exact D child，而不是只看current-host direct child；transient host在任何launch syscall前由独立existing-only/zero-write reader从fixed-D canonical history核对唯一latest `*_start_authorized`及当前role result／fixed aggregate／post-canary alias全absent，并在CreateJob／CreateProcess／ResumeThread前后fail-closed pinned checkpoint；产品调用图禁止会create layout的persistence factory与短读reader；service class exact override pywin32 `SvcRun/SvcInterrogate`。transient只在post-Resume journal/artifact checkpoint后报告RUNNING且child admission永闭；steady在START_PENDING完成static/Job/post-Resume prelaunch facts后报告RUNNING，继续持B2 lock/workspace完成RUNNING-only SCM→endpoint→writer全链、final facts与promotion，再由一次性prepare authorization向匿名pipe写PREPARE并只进入仍关闭的`ack_pending`；fresh fixed endpoint/writer readiness acknowledgement之后B2才派生不可重放commit authorization，COMMIT+EOF后child才`admitted`，post-commit observation通过后才unlock/wait。pre-COMMIT普通请求与listen backlog重排在最外层固定503且不触碰SQLite；prepare/ready-ack/commit/close/observation unknown均kill job，reader fatal必须退出child/whole Job。verified transient必须stop并fresh steady boot，不得原地开闸。host不取得transient controller lock、不写journal、不把journal升级为qualification，transient controller在RUNNING前保持B2 lock且不得POST或推进journal。

## 4. Stage 3：Reference 版本化编译与展示

- [x] 4.1 在现有 intake 上实现模式默认 publishable、supporting 分类和 reserved/draft/private/reparse/secret/结构/身份歧义 quarantine；将发布许可与 `external_ai_allowed/no_external_ai` 独立建模。
- [x] 4.2 实现 stable research/document identity、同路径 revision、纯移动 alias、歧义映射、显式 tombstone/replacement、历史访问和 per-snapshot `comment_anchor_projection`；只允许 exact unique span/hash+结构上下文或已验证 unchanged-block mapping 自动重定位。
- [x] 4.3 扩展确定性 IR：blocks/spans/heading/math/raw table/code/figure ref/citation/link；原 Markdown byte hash 保持不变。
- [x] 4.4 从 IR 生成稳定 heading-aware chunks：短内容严格一语义 block 一 chunk，公式/表格/代码/引用不跨界切分，只有超长 block 使用 parent/child，邻接只作 context；记录 chunk/source/range/version/chunker metadata，禁止相邻段落冒充 matched evidence。
- [x] 4.5 将 parse/render/link/chunk/lexical search/active membership 合并为 deterministic base snapshot；语义增强使用 `pending/ready/failed_retryable/blocked_policy`，DeepSeek 故障不得阻塞 base 激活。
- [x] 4.6 实现 changed-document-only 的 chunk/index/关系 backref 重建，以及 tombstone/deprecated 的默认召回失效；任一步失败不得激活页面/索引混合版本。
- [x] 4.7 在命名空间隔离的 generic renderer 展示新研究；以 `reference/archive/Q5/低SNR横截面选股_因子历史表示与压缩研究_结构重构扩展版.md`（SHA-256 `4994d1df74414fdadfefb7ba812c3851ef26fd82c36bc7f174c7db577e756679`）的 byte-exact、reference 外隔离 test-only 新身份，证明无需专用 route/template 自动展示 TOC、公式、宽表、代码、引用/locator、版本和已验证知识/pending，并完成 raw Markdown 任务对照；现有页面继续走 legacy renderer 并通过视觉/交互回归。

## 5. Stage 4：知识形成、检索与 MCP

- [x] 5.1 实现 `deepseek-v4-pro` changed-source-only job key、持久 job 状态和 targeted recompile campaign；job 绑定 requested alias、expected official provider revision 与 model-identity contract，普通发布不得全量重跑未变化 source versions。
- [ ] 5.2 按新发布面重新验证从现有受保护凭据注入 API key：证明 key/header 不进入 Git、日志、release manifest、candidate、active/prior binding、receipt、D-root audit 或回退材料；`private/no_external_ai` 不得构造 API 请求，且已取消的产品入口不能成为凭据旁路。
- [x] 5.3 实现 system/source-data 隔离、无工具/网络/secret、严格 JSON schema、允许 span 闭集和 prompt-injection adversarial fixtures。
- [x] 5.4 建立 source_explicit/model_candidate/machine_verified/human_reviewed/rejected/deprecated、knowledge generation 和字段级 source locator；记录官方 revision evidence URL/hash/observed-at、API 返回 model/system_fingerprint/response identity 及 prompt/schema/source/IR/output hashes。
- [x] 5.5 实现 provider identity drift 门禁：官方 alias 映射或 API model/fingerprint 变化且未能证明同 revision 时隔离输出，创建新 generation，并只通过显式 targeted recompile 选择受影响 source versions；禁止与旧 generation 混用。
- [x] 5.6 实现机械接受的 extractive/controlled-normalization 规则、人工接受入口、coverage report 与缺失/冲突处理；抽象摘要或推断关系不得仅凭 confidence 自动转正。
- [x] 5.7 实现 API 超时/失败/非法结构/越界 span/证据失配的无污染失败语义；timeout 必须是启动前由 immutable `part_count` 派生并记录的整体 wall-clock deadline（v1 为 `min(1800, 360*part_count)` 秒），由 parent 终止隔离 worker，不能只依赖 socket inactivity timeout。base snapshot 可先发布，验证完成后生成新的 enriched snapshot。compiler workspace 位于 Git 外受保护状态根，release 只密封正式投影与所选成功 generation；后续失败/pending job 保留上一成功 generation。同 commit/reference 下正式知识变化必须生成新的 snapshot/release/manifest。live campaign 已全 terminal，并以 SQLite 一致性副本提升为 Git 外单一 authority、保存 promotion receipt；发布/holdout 已改为严格 read-only store。一次 dry-run 误用可写 getter 导致物理 hash 漂移后已 fail closed、保留取证副本、从同一冻结一致性副本恢复 exact receipt hash，并以回归证明主文件与 sidecar 零变化。
- [x] 5.8 实现 exact/alias/FTS5/CJK/short fallback、item-scope applicability 与受控 facet alias、显式否定硬过滤、极性/条件安全 relation 扩展、版本/状态惩罚、重排与 exact source-range/knowledge-identity 去重；展示 context 不参与 qrel 命中或证据增权。
- [ ] 5.9 建立 source-version/span/exact-byte-range/quote-hash grounded qrels、覆盖矩阵、development set 与至少三分之一 sealed holdout；answerable 的 Recall/nDCG/MRR 与 no-answer accuracy 分开聚合，kind/citation 绑定实际正向卡；包含因子/模型/数据/回测、hard negative、无答案、条件冲突、历史/废弃和错引，来源修订后 qrel 自动 stale。
- [ ] 5.10 与当前 LIKE search 比较总体和分 slice 质量；预注册门禁，证明至少两个声明的 lexical/structured 困难 slice 稳定改善且引用/废弃/冲突硬错误为 0。
  - Round3 evidence-bound 合同已升级为 v4：exclusive-create/fsync prereg ledger 在评测前绑定 exact suite、两侧 live projection、producer extension/source receipt 和 limit；per-qrel v2 receipt 冻结 query/displayed bytes，由 verifier 对 supplied live suite/base/index 全量重放并双向重算 stale。资格基线无条件要求 exact `LikeBaselineIndex.from_archive_catalog` 只读 producer，在 compare 时再次核对 live database/WAL、closed ordered rows、document/page identity 与 presentation/source receipt；plain index、diagnostic Like、子类或伪 authority 均令 `projection_authority_pass=false`。当前仍缺全新 development/sealed suite 的真实资格结果，因此 5.9/5.10 保持未完成。
  - Round4 将 Archive authority 扩展为覆盖完整 compare 的双端窗口：live per-qrel replay 前冻结 database/WAL bundle、producer extension、source receipt identity；两侧全部 replay、receipt/metrics 重算结束后、构造 verdict 前再次执行 exact verifier。仅前后均 authoritative 且 identity byte-for-byte 相同才可通过；compare 前变化或 baseline search 窗口内变化均为非资格。未使用会误导为 writer lock 的普通 WAL read transaction，也未修改、写入或推进 Archive 原库的 WAL 状态。
  - Round5 闭合 exact verifier 自身的内部窗口：每次 verifier 都在入口与全部 artifact/receipt/presentation/rows/identity 校验后的末端分别复算 live database/WAL bundle，并要求入口、末端、source receipt 三者 exact；任一内部变化均 fail closed。Round4 的 compare 前后双端 verifier 继续保留。公开合成回归还把全部非 authority 质量条件固定为通过，证明末端变化独立令 `projection_authority_pass=false` 且最终 gate FAIL；这仍不是全新真实 development/sealed suite 的资格结果。
- [x] 5.11 依据召回/排序、no-answer、条件、版本、引用、P95、体积和重建成本联合决定 vector；本版不得依赖 vector 才可用。独立隔离的 pinned `multilingual-e5-small` 对照虽使 current candidate Recall 提高 0.0833，但 knowledge-kind accuracy 下降 0.0833、hard error 增 2、CPU P95 变慢且 peak RSS 约 1.72 GiB，因此本版明确拒绝生产 vector，保留 structured lexical；实验未读取 sealed qrels，authority/reference/Git 均未变化。
- [x] 5.12 实现经 tool-choice eval 验证的最小只读 MCP 工具面，以及客户端本地 `serve-stdio`、用户级 immutable mirror 与只读 VM authority resolver；current-sensitive 请求只有 mirror/VM `release_id/manifest_sha256/snapshot_id` 一致才返回 fresh，断网、落后、伪造或不可验证时返回结构化 stale/unavailable 并使 continuation 失效。
- [ ] 5.13 交付 cwd 无关、可幂等 install/doctor/uninstall 的 CLI/package、user/project Codex profile、`AGENTS.md`/server instructions 应调用与不应调用规则；在 `quant_platform` 和独立 `D:\quant\backtest_demo` 从无显式 MCP 字样的 prompt 验证 search→get、不应调用、snapshot activation/rollback 后 list-updates→重查与返回身份。本版不实现 HTTP。
  - 2026-08-22 冻结验收：install/doctor/uninstall、cwd 无关、隐式初始 search→get、无关任务零调用、fresh/stale/unavailable 和 exact identity 已有真实 CLI 证据；但初始出现一次非 search 返回 ID 的 get，v3 activation/rollback 分别为 35/20 次目标调用且各有一次非法 `budget_chars` get，超过预注册上限 6，故本任务保持未完成。去敏机器报告保存在 Git 外验收证据区，历史路径不再构成发布或 VM 存储合同。
  - 2026-08-22 独立 verifier 修订：mirror 已加入 durable `acknowledged.json`/`pending_transition.json`，`doctor` 不得确认或清洗转换；R1→R2→R1、跨 stdio restart、转换损坏 fail-closed 已有定向测试。profile 使用 fail-safe restartable 顺序：安装 client→AGENTS→config（最后激活），卸载 config→AGENTS→client（首先停用）；每个 crash cut 均机械证明 inactive 或 active 且依赖齐全。可捕获 `OSError` 才做原字节回滚；卸载只有 client 与 AGENTS 均恢复并复验成功后才恢复 config，任一前置回滚失败则保持 inactive 并保留 client tombstone，不宣称跨文件 crash-atomic transaction。marker END-before-BEGIN 也在写入前拒绝。历史真实 v3 由新机械 gate 仍判 FAIL（35 target calls、1 failed、2 个 get 无前置 search provenance），未重跑真实 Codex，故不改变本任务状态。
- [ ] 5.14 用相同任务做 MCP-assisted 与 no-MCP 对照；只有 grounded decision、条件/限制识别和引用正确性出现可复现净增益且无意义调用受控时才通过。
  - 后续只能使用运行前生成的封闭 `qrh-mcp-acceptance-preregistration/v2-bound`：绑定 authority/server/model/config/run/case、prompt bytes hash/length、tool-choice 顺序与预算，以及三项质量 marker 定义的 canonical bytes/hash；prereg 必须先以 exclusive-create + fsync 写入 durable ledger。每个公开 fake arm 只允许由 `FAKE_ONLY_REAL_CODEX_DISABLED` runner 在调用 fake transport 前写 dispatch intent、结束后写 trace-bound completion；最终 verifier 必须连同 ledger、exact prompt/config、两臂 raw JSONL 与 dispatch records 全量重放，不能只验证自报 campaign receipt。真实 Codex runner 尚未启用，fake-only PASS 不构成 5.13/5.14 资格证据。每维 marker 只接受 nonempty unique `list[str]`/`tuple[str]`，拒绝 string/bytes 标量及其自洽伪 envelope；final assisted response 还必须按 decision/condition/limitation 使用完整 object/document-version/source/span/byte-range/citation tuple。历史 v1/v2/v3 结果保持不可重算 FAIL；禁止回填 marker/prereg 或据此补造 PASS。
  - Round3 raw trace 状态机独立记录 `agent_message_seen` 与 completed count；每条 trace 恰有一条非空 final agent message，完成时无其他 open item，之后只允许 turn terminal。空消息、第二条消息或 final 后续 item 全部 fail closed。
  - 2026-08-22 冻结验收：相同 prompt 对照仅 citation correctness 从 0.0 提升到 1.0；grounded decision 与条件/限制识别分别为 0.50/0.25 对 0.50/0.25，净增益均为 0，不满足每维至少 0.05，故不得以“发生了调用”代替质量 PASS，留待全新独立多任务 suite 验收。

## 6. Stage 5：全局验证与放行

- [ ] 6.1 回放全部现有 Archive、Evidence、Paper Lab、两族 comment、Dashboard/workspace 与完整 resource inventory；增加真实非空 comment 序列“v1 写入 document/exact-block/edited-span comment→新代码→source 修订与移动→renderer→D prior 回退→再读取”，用浏览器和 SQLite 同时验证正确重定位、history/unresolved 可见、无错挂及 current/event/revision/actor/time 不变。
- [ ] 6.2 验证新增、修订、移动、删除、DeepSeek 失败/超时/非法输出/官方 revision 或 fingerprint 漂移/模型升级、重复执行、并发 publish、网络中断、active/binding 损坏、prior 不兼容、回退与清理异常路径。
- [ ] 6.3 验证 Web/Search/MCP 同一 snapshot、默认当前版本、历史/废弃语义、引用 locator 和 source bytes 零改动；从独立项目模拟 VM activation/rollback，证明本地 mirror 识别三元组变化、旧 continuation 失效并重新 search→get，网络不可达/过期/identity mismatch 不静默返回旧知识。
- [ ] 6.4 由独立 verifier 检查真实代码、机器结果、浏览器、状态和回退，不以执行者总结或旧 PASS 代替证据。
- [ ] 6.5 对 SQLite schema 升级后的 candidate/prior 做读写/CAS/event 兼容实测；普通回退沿用当前 D state，不执行 down-migration。
- [ ] 6.6 对功能完整最终 release 真实演练 active→prior→active 的本地版本序列：每次切换均校验 exact manifest/snapshot、代码/前端、内容、PDF/图片/对象、Search/MCP、浏览器/API 和当前 SQLite state；证明回退不替换 state、不降 schema、不重启 C writer，也不读取 D 根之外的项目材料。
- [ ] 6.7 验证终态 retention roots 只包含 active R 与恰一 prior R；活动 journal 允许的瞬态 candidate 结束后必须清理，更早 prior、completed incoming/partial 与不再被两棵 closure 引用的对象按精确路径审计删除。第三 release、缺 binding、binding/active 漂移或清理失败均阻止下一 publish。
- [ ] 6.8 形成中文 runbook、bootstrap/cutover/local-prior/rollback/cleanup 演练、质量报告和 Stage 5 release certificate；证书须明确能力只覆盖 exact D 根及当前 state 完整时的最近一代版本回退，不得扩大到 VM、D 根、对象库或 state 整体丢失。
- [ ] 6.9 对 source、fresh installed wheel、CLI、config、schema、Windows 任务清单、runbook 引用和 VM write-set 做撤销面验收：不存在周期性状态副本任务、D 根之外的项目存储或不属于 active/prior 的保护 authority；旧 public symbols/entry points 必须不可发现、不可导入、不可调用，且产品包不携带对应实现。
- [ ] 6.10 用 schema/graph/hash fixture 证明唯一依赖方向 `active_release→R_active`、`local_prior_binding→R_active/R_prior`，activation/rollback、failure、cleanup receipt 分别只指向结果 pair、operation + 原 pair + target candidate、保留 pair + 精确移除目标及结果；构造 operation/failed-phase 与 journal 错配、activation target 复用原 pair、rollback target 非 exact prior、`R→active/binding/receipt`、receipt-as-active、多个 prior、binding/active 漂移、回退替换 state 与选择第三 release 的反例均 fail closed。

## 7. Stage 6：Public→Private 最终关闭

- [ ] 7.1 仅在 Stage 5 release certificate 后将 GitHub repository 从 Public 转为 Private。
- [ ] 7.2 重新核验实际 plan、Actions、branch/environment protection、CI、publish CLI 最小权限和 exact-SHA candidate 能力。
- [ ] 7.3 在 Private 状态运行一次 CI 与一次无生产切换 candidate 演练，确认冻结、传输、校验和权限链未破坏。
- [ ] 7.4 形成 visibility-closure receipt；Private 复验通过前不得标记项目最终完成。
